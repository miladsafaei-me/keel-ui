{
  "id": "before_after",
  "name": "Before / After Split",
  "category": "compare",
  "pack": "core",
  "version": 1,
  "when_to_pick": "Show two states side-by-side — strategy before/after an optimization, broker A vs broker B execution quality, bad vs good risk management. Each side can contain inline HTML (lists, mini-tables, callouts).",
  "tags": ["before-after", "comparison", "transformation"],
  "audience_fit": ["forex", "crypto", "prop-firm"],
  "dependencies": [],
  "schema": {
    "type": "object",
    "required": ["before", "after"],
    "properties": {
      "before_title": { "type": "string", "default": "Before", "maxLength": 80 },
      "after_title":  { "type": "string", "default": "After", "maxLength": 80 },
      "before":  { "type": "string", "maxLength": 1500, "description": "HTML allowed." },
      "after":   { "type": "string", "maxLength": 1500, "description": "HTML allowed." },
      "caption": { "type": "string", "maxLength": 200 }
    }
  }
}
